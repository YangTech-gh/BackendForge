production:
  dispatchers:
    - polling_interval: 1
      batch_size: 500
  workers:
    - queues:
        - critical
        - default
      polling_interval: 0.1
      threads: 3
    - queues:
        - mailers
        - low
      polling_interval: 0.5
      threads: 2
